// Re-export the NotesProvider implementation which lives under lib/screens.
// This keeps the public import path `package:noteker/providers/notes_provider.dart`
// stable while the implementation is colocated in `lib/screens`.
export 'package:noteker/screens/notes_provider.dart';
